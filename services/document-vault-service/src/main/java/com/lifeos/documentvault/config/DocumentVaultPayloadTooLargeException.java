package com.lifeos.documentvault.config;

/** Raised when a chunked request exceeds the configured service-wide body admission limit. */
public class DocumentVaultPayloadTooLargeException extends RuntimeException {
}
