package com.lifeos.profile.domain;

/** Profile data visibility is private by default and can only extend to an explicitly scoped household. */
public enum ProfileVisibility {
    PRIVATE,
    HOUSEHOLD
}
