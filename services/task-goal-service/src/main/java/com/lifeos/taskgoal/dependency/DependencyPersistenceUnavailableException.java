package com.lifeos.taskgoal.dependency;

/** Bounded graph-lock or persistence work could not complete safely; clients may retry. */
public class DependencyPersistenceUnavailableException extends RuntimeException {
}
