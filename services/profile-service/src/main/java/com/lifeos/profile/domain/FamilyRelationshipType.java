package com.lifeos.profile.domain;

/** Explicit, user-managed household or family relationship classification. */
public enum FamilyRelationshipType {
    SELF,
    SPOUSE,
    PARTNER,
    CHILD,
    PARENT,
    SIBLING,
    OTHER
}
