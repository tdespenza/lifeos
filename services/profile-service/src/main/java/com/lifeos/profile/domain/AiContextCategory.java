package com.lifeos.profile.domain;

/** A bounded, consented category that a future AI integration may use for personalization. */
public enum AiContextCategory {
    PROFILE,
    PREFERENCES,
    GOALS,
    JOURNALS,
    ANALYTICS,
    DOCUMENTS
}
